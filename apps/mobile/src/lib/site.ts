/**
 * Public web origin — shared section links point here so recipients who don't
 * have the app can still read the law. Update when the real domain lands
 * (mirrors apps/web's SITE_URL).
 */
export const WEB_URL =
  process.env.EXPO_PUBLIC_WEB_URL ?? "https://vidhara-web-lyart.vercel.app";
