# NexLex — Decision Log

> **Status**: Living document — append-only. · **Last updated**: 2026-07-19
> Product/strategy decisions with rationale and a revisit trigger. Architecture decisions stay in `architecture.md` §16 (ADR-1…11); this log covers product strategy from the 2026-07-16 lean reset onward. Updated the day a decision is made — not at release boundaries.

---

**D-001 · 2026-07-16 · Adopt release-based lean roadmap; phases.md superseded for forward planning.**
Context: Phase 1–8 plan deferred user contact by months and pre-committed features on zero evidence; founder called it out explicitly. Decision: releases V0.1→V2.0 with pre-registered gates ([release-plan.md](release-plan.md)); phases.md retained as historical build log only. Revisit: never (direction), quarterly (calendar).

**D-002 · 2026-07-16 · One wedge, one customer: old⇄new law mapping for law students & judiciary aspirants.**
Context: the vision spans ≥4 distinct products/customers. The 2023→new-codes transition is an urgent, universal, *expiring* pain in the student/aspirant segment, and our 1,271-mapping corpus is the only clean asset addressing it. Decision: everything not serving this wedge is FUTURE or REMOVE ([feature-priority.md](feature-priority.md)). Advocates are an expansion segment, not a launch segment. Revisit: G1 pivot review if A-1 fails; also if organic *advocate* usage appears unprompted.

**D-003 · 2026-07-16 · Web-first for validation; Android runs in parallel, launches at V1.0.**
Context: ADR-8 says Android-first — correct for the long-term product (audience is mobile-first), but Play's 12-tester × 14-day closed-test requirement gates a personal account's production access, while the web (already SEO-structured) deploys in days and shares cleanly into Telegram/WhatsApp. Decision: learning happens on web from V0.1; the Play clock starts at V0.2 so Android production lands inside V1.0. This amends ADR-8's *go-to-market order*, not the architecture (Expo app remains the flagship surface). Revisit: if web traction is blocked by mobile-web friction in beta interviews.

**D-004 · 2026-07-16 · No AI before Gate G1; first AI is a scoped, eval-gated, section-grounded explainer.**
Context: AI Tutor is the vision centerpiece, but wrong legal answers to exam-takers are trust-fatal, and a general tutor has unbounded scope/cost. Decision: fake-door demand test first (V0.5); if chosen at G1, ship only behind the E-7 eval bar (~0 factual errors on a 50-pair hand-checked set), answers always citing + displaying the underlying section; kill-switch feature flag; Sev-0 on any user-reported factual error. Revisit: at G1 with data.

**D-005 · 2026-07-16 · Kill list moved to parking lot with unlock conditions.**
Simulator, Moot Court, AI Judge, Virtual Internship, Advocate Workspace, Drafting, Community, Leaderboards, Achievements, Calendar, user-facing Analytics — each is a different startup or a post-PMF luxury ([future-ideas.md](future-ideas.md) has per-item reopen conditions). Revisit: only via those conditions.

**D-006 · 2026-07-16 · No monetization before Gate G2.**
Charging before retention measures churn speed, not willingness-to-pay. Free tier permanently includes the full corpus + mapping (acquisition engine, public good, SEO moat); the paid layer will be whatever V1.1/V1.2 *proves*. Old pricing guess (₹149/₹399) retired until E-8 pricing interviews. Revisit: G2 (2026-11-03).

**D-007 · 2026-07-16 · No account requirement to read; bookmarks/recents are local-first.**
A signup wall in front of an unproven product measures friction tolerance, not demand, and suppresses the retention signal we exist to measure. Accounts arrive when they carry user value (sync) or business necessity (payments, V2.0). Revisit: if local-bookmark loss (device switch/clear) surfaces as a top-3 interview complaint.

**D-008 · 2026-07-16 · Analytics stack: PostHog (cookieless, anonymous) + Sentry + UptimeRobot; event schema versioned in analytics-plan.md.**
Free tiers, zero infra, sufficient through V2.0. Instrumentation breakage is treated as an outage (a blind week = a lost week of learning). Raw lookup/search text never enters analytics (privacy). Revisit: at event-volume or team growth pressure.

**D-009 · 2026-07-16 · Beta design: 30–50 recruited users, 3-week window, D7 ≥25% bar, feature freeze during the window.**
Small enough to talk to everyone, large enough for cohort math to mean something; warm-cohort D7 below 25% ≈ absent pull. Freeze because shipping mid-window confounds the cohort. Revisit: n/a — one-shot design, judged at the V0.5 gate.

**D-010 · 2026-07-16 · Fake doors permitted, with honest copy only.**
"Coming soon — tap to tell us you want this" (no fake functionality, no bait UI that pretends to work). Measures curiosity cheaply; always cross-checked against interviews before money is spent (E-5). Revisit: if users express annoyance in feedback (then stop immediately — trust > signal).

**D-011 · 2026-07-16 · Content policy: source-faithful text, bundles as artifacts of record, corrections are Sev-0.**
The corpus is the moat and the trust anchor. India Code's own body-heading typos are preserved (documented in phases.md 2026-07-16 entries) pending an editorial-emendation policy; any user-reported *parsing* error outranks feature work. New acts are added on demand-evidence, not completionism (pipeline makes each act ~days). Revisit: emendation policy decision when search-miss data or user reports force it.

**D-012 · 2026-07-16 · Documentation cadence: decision-log same-day; release docs at release boundaries; docs never block a ship.**
Ten strategy docs + six build docs is a lot for a solo founder; the failure mode is documentation-as-procrastination. The only artifact that matters in any given week is the shipped release and the Monday numbers. Revisit: if docs drift is causing real mistakes (then simplify the doc set, don't write more).

**D-013 · 2026-07-16 · Observability accounts deferred by founder; V0.2 build proceeds; Vercel deploy flagged as the one deferral that hollows V0.1.**
Context: founder deferred PostHog/Sentry/Vercel/UptimeRobot account creation. Decision: acceptable for the three observability tools — all code no-ops without keys and activates by pasting env vars; NOT neutral for Vercel, which *is* V0.1 (a product nobody can reach isn't released) — recorded as the standing top founder action. Build continues into V0.2 so the eventual single deploy ships V0.1+V0.2 together. Cost accepted: friendly-user walkthroughs run without dashboards (observation only). Repo pushed to github.com/KrishBahukhandi/Vidhara. Revisit: the moment the Vercel import happens (then wire keys, verify live events, tag v0.1.0).

**D-014 · 2026-07-16 · Product renamed NexLex → Vidhara (umbrella: Bahukhandi Labs); nexlex.in retired.**
Context: on first deploy, `nexlex.in` (the hardcoded default SITE_URL) turned out to belong to an **unrelated law firm** ("NexLex — Best Lawyer in Indore"), and it soft-404s our paths with HTTP 200 — so every share link, canonical tag, and OG image was silently pointing at a competitor's domain (broken growth feature + SEO own-goal). Founder decision: rename the product to **Vidhara** (already the repo + Vercel project name), under a future **Bahukhandi Labs** umbrella (a domain like vidhara.in / *.bahukhandi-labs will be bought later). Scope of rename: **user-facing strings only** — site header/footer, page titles + metadata, OG cards, privacy page, section/mapping copy, health `service` field. **Internal `@nexlex/*` package names, the Supabase project ref, and `createNexlexClient` are deliberately NOT renamed** — code-internal, zero user value, and a monorepo-wide import churn/risk with no upside now (revisit only if it ever confuses a new engineer). Interim SITE_URL default = the Vercel deployment URL so share/canonical/OG work immediately; override `NEXT_PUBLIC_SITE_URL` when the real domain lands. Docs still say "NexLex" in prose — a cosmetic sweep, not load-bearing; deferred so it doesn't gate the release. Revisit: when the domain is purchased (set env var + custom domain in Vercel).

**D-015 · 2026-07-17 · Chapter titles repaired corpus-wide (text rule + curated set); suggestions channel opened.**
Context: chapter titles carried small-caps drop-cap artifacts ("A RREST OF PERSONS"), and — worse — 56 rows (CrPC 35, IPC 17, IEA 2, COI 5, + 4 running-head garbage) had initials-only or junk titles from running heads; the source PDFs are no longer on disk so a parser re-ingest wasn't possible. Decision: (a) `normalizeChapterTitle()` in gazette-common (two-pass: drop-capped "O F"→"OF", then solitary-capital+≥2-token join; punctuation tidy) applied in both parsers + 5 unit tests — future ingests are clean; (b) same rule applied to bundles + live DB (75 rows); (c) **curated titles** for the initials-only rows — CrPC 35 + IPC 17 chapter names from the statutes, each **corroborated against the attached section ranges and first marginal notes in our own DB** (e.g. "O O H B" at §299 Culpable homicide = OF OFFENCES AFFECTING THE HUMAN BODY); IEA/COI got a mechanical running-head-suffix strip. This is a narrow, verifiable exception to "no content from model memory" (D-011/ADR-6): chapter *names* are famous, low-risk, and cross-checked against ingested data; mappings/statute text remain strictly source-only. Known theoretical edge of the text rule ("A NEW …") documented in code; audit new acts' chapter diffs on ingest. Also: migration 0006 adds `feedback.kind` ('rating'|'suggestion', score nullable with shape check) powering the /feedback page (web) + Profile suggestion form (Android) — the founder-requested "tell us what to improve" channel. Revisit: if a future re-ingest with PDFs disagrees with any curated title, the parser wins.

**D-016 · 2026-07-18 · Bug-hunt pass (no new features): 1 high-impact bug + 3 smaller fixes; 2 advisors accepted-by-design.**
Systematic endpoint/advisor sweep on the founder's request. Found + fixed: (1) **HIGH — constitutional article lookups 404'd**: `parseSectionRef` returns the abbreviation "COI" but section routes use the slug `constitution` (slug≠abbr only for COI). The 3 web redirect sites (search page, search-box, landing-lookup) did `act.toLowerCase()` as the slug → `/acts/coi/21` 404. Fixed with a canonical `ACT_SLUG` map in @nexlex/shared (single source of truth; +2 regression tests). Mobile was unaffected (navigates via DB-sourced slug). (2) `search_sections` mutable search_path → pinned `pg_catalog, public` (migration 0008; advisor 0011 cleared). (3) `new URL(source_url)` in SectionProvenance could crash the section render on a malformed URL → try/catch fallback. (4) FeedbackFab applied safe-area inset as both padding AND margin (warped pill height on notched phones) → margin only. **Verified NOT bugs**: search input (websearch_to_tsquery is injection-safe, never throws on malformed input); `?about=` XSS (React escapes); OG image for missing section (fallback, no crash); sitemap slugs (DB-sourced, zero wrong /acts/coi/); landing concept→/search fallthrough (works; a browser-automation input desync made it look broken). **Accepted-by-design advisors (WARN, not fixed)**: pg_trgm-in-public (moving it means rebuilding trigram indexes for ~0 gain); feedback INSERT WITH CHECK(true) (intentional anonymous write-only box — anon has INSERT only, content bounded by CHECK constraints; spam is a rate-limit concern, not RLS). Revisit: add ACT_SLUG entry whenever a new act's slug ≠ lowercased abbreviation.

**D-017 · 2026-07-19 · Content-trust proofread: stripped 176 leaked headings from section bodies (live DB + bundles); dropped-illustrations bug logged for re-ingest.**
Context: spot-check of the most-cited sections (starting with the reported IPC §420 leak) exposed a *systematic* parser bug — group/part/chapter headings that belong to the *next* section leaked onto the END of the prior section's body ("…liable to fine. **Of Fraudulent Deeds and Dispositions of Property**"). A conservative sweep found **176** instances across all 8 acts: "Of …" group headings (IPC/BNS), lettered sub-parts "B.—…" (CrPC/BNSS), bracketed "[CHAPTER …]/[PART …]" markers, and garbled "C II.—…" chapter abbreviations (COI/IEA). Decision: strip them surgically — regex removes only the trailing heading, **keeping** the sentence terminator and any "Illustration(s)" marker (so genuine missing-content stays visible). Applied to `scripts/ingest/bundles/*.json` (artifacts of record) AND the live DB via idempotent, id-preserving, `version`-bumping `UPDATE`s (residual scan → 0). All 40 ingest tests green; bundles re-validate. **Second bug logged, NOT fixed here (needs re-ingest):** the inline/bbox parser's small-font footnote filter also dropped **illustration text** — **153 sections** (IPC 64, ICA 88, CrPC 1) now end at a bare "Illustration(s)" heading with the examples gone (e.g. IPC §300 murder, §378 theft). No source PDFs are committed, so this requires re-download + an illustration-aware parser pass. Gotcha re-confirmed: bundle slug `coi` ≠ DB slug `constitution` (the 52 COI updates no-op'd until re-run with the DB slug — see D-016). Note: section pages are ISR-cached 1h with no on-demand revalidate route, so a content fix shows on fresh renders immediately but cached pages lag ≤1h. Revisit: schedule the illustration re-ingest before V0.5 cohort; consider an on-demand `revalidatePath` route so content fixes go live instantly.

**D-018 · 2026-07-19 · Illustrations restored (178 sections, +~127 KB of statute text) via illustration-aware re-ingest of IPC/ICA/CrPC; parity-checked, zero regressions.**
Context: D-017's proofread found the deeper bug — `gazette-inline.ts` dropped every word under 8.6pt to kill footnotes, but ILLUSTRATIONS are set in the same ~8.1–8.2pt type, so the worked examples vanished from 153+ sections (IPC §300 murder, §378 theft, §415 cheating, ICA §23…). Height cannot separate the two; **position and shape can**: illustrations follow an "Illustration(s)" heading and end at the next body-height line; footnotes sit at page bottom and open "N. Subs./Ins. … by Act …". Decision: parser keeps small-type lines only inside an illustration block (heading→body-line state machine; survives page breaks — 25 blocks wrap pages; page-scoped footnote latch verified against all 182 footnote blocks incl. 11 direct collisions; letterless small lines = superscript refs, always dropped; `keepIllustrations:false` escape hatch replicates legacy byte-for-byte). Sources re-acquired identically (ICA/CrPC byte-identical from indiacode via dns.google→Akamai; IPC from the exact Wayback snapshot 20200421003546); **parity check: keep=false output == pre-D-017 bundles across all 1,386 sections — 0 mismatches**, so the merge could safely take the new bodies (D-015/D-017 curation re-applied post-parse and preserved). Result: 178 sections gain their illustrations (IPC 74, ICA 97, CrPC 7; 1,243 lines, ~127 KB); IEA unaffected (its PDF sets illustrations at body height). Published via the repo's edge-function pattern (v16 one-time-token delta publisher → 178/178 rows, version+1 → tombstoned v17); provenance updated on all 3 acts (bundle + DB). Corpus-wide scan: **0 sections end at a bare "Illustration(s)" marker**. 6 new parser unit tests (46 total). Note: ICA prints a few footnotes at full body height — those were always kept (pre-existing, source-visible; parity-covered), a proofread item for later, not a regression. Revisit: run the D-017/D-018 tail-scan + bare-marker scan as standing checks in the V1.0 random-50 content QA.

**D-019 · 2026-07-24 · First AI feature shipped — "Explain this section", grounded + cache-first + provider-swappable; Gemini (free) not Anthropic; dark until the key lands.**
Context: D-004 gated AI to a scoped, eval-gated, section-grounded explainer; the founder chose a **free** provider (Gemini `gemini-2.0-flash`) over Anthropic to avoid per-token cost at scale, and approved building it now. Grounding is the whole game (wrong legal answers are trust-fatal, D-011): the Edge Function `explain-section` fetches the section's own published text + its old↔new mapping **server-side** and passes ONLY that to the model — the client sends `{slug, number}` and cannot inject text; the SYSTEM prompt forbids outside law, advice, and invented cross-references; the statute text stays on-screen beside the explanation with a "verify against the official text · not legal advice" disclaimer. Decision: (a) **cache-first** makes the free tier viable at scale — a section's text never changes, so it's explained once (first viewer) into `ai_explanations` and served to everyone after; cache hits cost nothing and consume no quota. (b) A global per-day counter `ai_usage` (`AI_DAILY_CAP`, default 800) soft-caps generation to protect the free quota; over cap → friendly 429. (c) Provider isolated in `callLLM()` (ADR-5) for a one-function swap. (d) `GEMINI_API_KEY` is a **Supabase Edge Function secret** — never in git/clients (rules §11); until it's set the function returns 503 and the UI shows "AI explanations are being set up." — so the feature ships dark and **activates the moment the founder adds the key**, no redeploy. Migration 0009 adds both tables, RLS-on with **no anon policies** (service-role only). Reader UI replaces the `ai_explain` fake door on **both** web + mobile with the real button + `ai_explain_requested` analytics event; a `pnpm --filter @nexlex/ingest ai-eval` harness samples real published sections across all 8 acts, prints source-beside-explanation for human grounding review, and red-flags likely-invented cross-references — the accuracy gate before the feature is trusted. All three packages typecheck clean; web round-trip verified live (click → event → live function → 503 → friendly message rendered). Revisit: **run `ai-eval` and eyeball the output the moment the key is set** — do not trust it until the eval passes; tune `AI_DAILY_CAP` off real usage; the fake-door demand signal on `ai_explain` ends here (this replaces it).

**D-020 · 2026-07-24 · Explainer provider swapped Gemini → Groq (Llama 3.3 70B); eval passed, feature is LIVE.**
Context: with the key in place, Gemini rejected **every** call with `429 RESOURCE_EXHAUSTED, generate_content_free_tier_requests limit: 0, model: gemini-2.0-flash` — the Google project behind the (valid, authenticating) key simply had **zero** free-tier quota (created in a project not enrolled in the free tier / billing-linked). Not a code bug: we reached Gemini fine. Rather than enable Gemini billing, the founder chose to stay free via **Groq**. Decision: point `callLLM()` at Groq's OpenAI-compatible chat API (`api.groq.com/openai/v1/chat/completions`, `Authorization: Bearer GROQ_API_KEY`, model `llama-3.3-70b-versatile`, env-overridable via `GROQ_MODEL`) — a ~1-function change exactly as ADR-5's provider isolation intended; grounding, cache, daily cap, disclaimer, and the web+mobile reader UI are byte-for-byte unchanged. `GROQ_API_KEY` replaces `GEMINI_API_KEY` as the Supabase secret (old one now ignored, safe to delete). Also hardened while here (survives future provider swaps): a provider non-2xx/empty completion used to bubble up as a 500 with the **raw provider JSON** in the client body — now `callLLM` throws a typed `ProviderError` (429 → 503 "busy, try again"; other → 502 "try again"), the full provider error is `console.error`'d server-side only, and the cache upsert still runs only on success so a failed generation never poisons the cache. **Eval PASSED**: `ai-eval` across 2 sections/act × 8 acts = 16 real generations, **16 explained, 0 red-flag hints, 0 failed** — every output grounded in the section's own text, legal terms defined (surety, power-of-attorney, local jurisdiction, secure electronic record…), no case law, no advice, no invented cross-references; live web round-trip re-verified (button → "In plain language" card renders bullets + takeaway + disclaimer, statute text stays above). The feature is now live for everyone (dark-ship gate lifted). Revisit: watch Groq free-tier rate limits (30 req/min, ~1k/day for 70B) against real traffic — the cache means each section generates once, so limits bite only on a first-view spike; if quality dips on edge cases, `GROQ_MODEL` swaps the model with no redeploy of logic; re-run `ai-eval` on any prompt/model change.

**D-021 · 2026-07-24 · Auth email (email-OTP sign-in) PARKED until custom SMTP; app ships fully usable signed-out.**
Context: on-device sign-in delivers a magic **link**, not the 6-digit code the app expects, so no one can complete OTP. Root cause is **not code** — `requestOtp`→`signInWithOtp` and `verifyOtp` (expecting the `{{ .Token }}` 6-digit code) are correct (apps/mobile/src/features/auth/api.ts, verify.tsx). It's Supabase config: the **built-in email service locks the template editor** ("Set up custom SMTP to edit templates") and its default Magic Link template emits `{{ .ConfirmationURL }}` (a link). It's also rate-limited to a few/hour — unusable for a 30-person beta regardless. Decision: **defer** — the founder will connect custom SMTP later, after buying a domain, via Google/Gmail SMTP (caveat: ~500/day cap + app password/2FA; a transactional provider like Resend/Postmark is sturdier for beta deliverability/volume). Fix when ready = add SMTP → edit **Magic Link** + **Confirm signup** templates to include `{{ .Token }}` → verify the OTP round-trip; **no app code change**. Not a blocker: `index.tsx`→`/(tabs)/library`, sign-in is optional (Profile tab only, adds cross-device sync; D-007) — the whole product (browse, mapping, search, AI "Explain") works signed-out, so beta build/testing proceeds now. Revisit: required before any account-gated feature or before relying on sync; recorded in agent memory `auth-email-otp-deferred`.

**D-022 · 2026-07-24 · Daily MCQ shipped early (V1.1 candidate B) — corpus-grounded, deterministic, replaces the fake door.**
Context: like the AI explainer, the founder chose to build the Daily-MCQ candidate ahead of G1 for the exam-habit loop (highest-pain segment: judiciary aspirants). **Zero hallucination by construction** (the whole point of picking this candidate): the `daily-mcq` Edge Function builds each question ENTIRELY from the authoritative NCRB old↔new mapping (`v_mapping_lookup`) server-side — "which new-law section corresponds to old §X (marginal note)?", correct answer = the mapped counterpart, 3 distractors = other real sections of the **same** new act. Only clean, unambiguous mappings are used (`renumbered`/`identical`/`modified` — verified **0 ambiguous sources** across 954 rows, so every question has exactly one right answer); merged/split/omitted/new excluded. **Deterministic per IST day** (index = dayNumber mod pool, mulberry32-seeded shuffle) so everyone gets the same question → a shared, shareable daily habit. Delivery: web `/daily` page + "Daily Quiz" nav link; mobile `/daily` screen + a home card (in the Library header) replacing the `daily_mcq` fake door. **Streak** is local-first (`vidhara_mcq`, same posture as recents/bookmarks; one answer/day, persists, locks — can't re-answer). Analytics `daily_mcq_viewed`/`daily_mcq_answered` on both platforms. **No new migration** (reuses the corpus). Grounding cross-checked (IEA §30 → BSA §24 verified against the DB); web round-trip verified live (correct answer → green ✓ + 🔥 streak + explanation + "Read §" link; answered state persists on reload; 0 console errors); both packages typecheck clean. Revisit: at G1 compare MCQ vs the AI explainer vs Offline on real engagement (both AI + MCQ fake doors are now retired into real features, so G1 reads *adoption/retention of what's built*, not fake-door CTR); add a curated conceptual-question bank (new table) only if interviews pull for questions beyond mapping-recall — until then, corpus-derived only (no authored legal content, D-011).

**D-023 · 2026-07-24 · "Ask → routed to the real law" (grounded AI librarian); explicitly NOT an answer bot — D-004 line held.**
Context: founder wanted to expand the AI beyond section-explaining — "ask different things", surface it on home, and (their words) *"instead of just giving the answer it redirects the user there / gives them the article to read."* Decomposed into (a) grounded routing/retrieval vs (b) open-ended legal Q&A. (b) is the exact *"biggest trap"* D-004 names (wrong legal answers are trust-fatal) and is gated behind proven retention in feature-priority.md — declined for now. Founder chose (a). Decision: an `ask` Edge Function whose model has ONE job — translate a layperson's phrasing into formal **statutory search phrases** (e.g. "anticipatory bail" → "bail to person apprehending arrest"). Those phrases run against the REAL corpus (`search_sections` RPC), so **every result is an actual, linkable section** — the AI is a librarian, never an oracle: it never answers the legal question, never asserts a section number, and degrades to plain FTS if the model is down. Wiring: web `/search` runs plain FTS **first** (fast, no AI/no cost) and only calls `ask` when FTS returns **zero** (the NL/colloquial-term case), then renders the real sections with an *"interpreted as '<phrase>'"* transparency line + "read the section itself to confirm". Home `LandingLookup` + `SearchBox` copy now invite questions ("anticipatory bail", "punishment for cheating"); exact refs still route directly (`parseSectionRef`, no AI). Mobile Library search has the same FTS→`ask` fallback + note (reuses HitRow). Analytics `ask_ai_assisted {found}` both platforms; page reframed "Ask or search". Verified live: "anticipatory bail" → CrPC §438 / BNSS §482 (which plain FTS misses entirely); "cheating" stays on the fast FTS path (no AI). Honest limit: retrieval is grounded, not omniscient — a weak match surfaces a visibly-off real heading, **never a false legal claim** (that's the safety win vs an answer bot). Revisit: if retrieval quality becomes the bottleneck, add pgvector semantic search; the open-ended answer bot stays gated behind retention (D-004) — build it on data, not enthusiasm.

**D-024 · 2026-07-25 · Quiz becomes an engine: unlimited Practice mode + 3 question types (funding-gap fortnight, week-1 work).**
Context: founder is short of funds for Play Console / domain / SMTP for ~2 weeks and asked what adds value meanwhile. Reframed: **money gates Android distribution, not the beta** — the web app is live and free, so recruitment into judiciary-prep Telegram / law-college WhatsApp groups can start now at ₹0, and the fortnight goes into depth. Founder also asked whether MCQs are "fixed to 9-10" — clarified they were **already** auto-generated from all 954 clean mappings (~2.6 years of unique dailies); the real gap was *only one per day*. Decision: generalise `daily-mcq` into a **quiz engine** — `mode: "daily" | "practice"` (daily stays deterministic per IST day = the shared habit anchor; practice = unlimited random with an `exclude` list so a session doesn't repeat) and **3 grounded question types** rotating daily / random in practice: `forward` (old→new, the core wedge), `reverse` (new→old), `subject` (topic→section, asked on the new law since that's what exams now test). Still **zero-hallucination**: every question, answer and distractor is a real row/section from the NCRB mapping — distractors are always other real sections of the *same* act. Delivery: shared `McqCard` on both platforms; web `/practice` page + cross-links from `/daily`; mobile `/practice` screen + "Keep practising" CTA. Session score on practice; the daily streak is unchanged. Analytics `practice_started`/`practice_answered`; `daily_mcq_*` now carry `type`. Verified live: daily deterministic across 2 calls; practice varied in question *and* type across 6 calls with `answerIndex` always correct; grounding cross-checked in the DB (CrPC §225→BNSS §248, §399→BNSS §440, §401↔BNSS §442, §346→BNSS §385, IPC §128→BNS §156); web practice loop (answer → score → next question) and the streak increment (seeded 3-day → 4-day) both verified in-browser; both packages typecheck clean (expo-router typed routes regenerated for `/practice`). Note: changing the engine mid-day can show a returning user a stale highlighted choice against that day's new question — a one-time migration artifact, not a user-facing flow. Revisit: remaining fortnight plan is 🅑 content-trust proofread → 🅒 SEO/share polish → 🅓 web-beta onboarding; add act/topic filters to practice only if interviews ask.

**D-025 · 2026-07-25 · Content-QA scanner + 85 repealed-section repairs; ICA §2/§3 boundary failure logged, NOT guessed.**
Context: funding-gap fortnight, item 🅑 — content trust is the moat and the clause-level proofread was still pending. Built `content-qa` (`pnpm --filter @nexlex/ingest content-qa`), a repeatable scanner over all **3,118 published sections** covering the defect classes we've actually hit (D-015 drop-caps, D-017 leaked headings, D-018 dropped illustrations) plus new ones (mojibake, control chars, empty/short bodies, unbalanced brackets, duplicate bodies, numbering gaps). **Scanner bug found and fixed first:** it paginated on `sort_key`, which has only **770 distinct values across 3,118 rows** — a non-unique sort key silently duplicates and skips rows between `.range()` pages (it manufactured a phantom "IPC §118 duplicates itself"). Now ordered by unique `id`. Also tightened `bare-illustration`, which false-positived on IEA §21 (2,813 chars of real prose that merely *ends with the word* "illustration") — so **the D-018 illustration fix has not regressed: 0 real cases**. Headline results: **no** encoding damage, **no** control characters, **no** empty bodies, **no** leaked headings, and **BNS/BNSS/BSA/COI are 100% clean of footnote debris** (the wedge content is unaffected). One real systematic defect: **84 repealed/omitted sections** (ICA 72, IPC 8, COI 4) had the repeal citation split at the wrong point — heading ended mid-citation ("…document showing title. Rep") with the tail stranded in the body ("by s. 65,ibid."). Decision: repair mechanically — move the citation from heading to body and normalise spacing — applied to the **live DB (version-bumped, `body_md` updated too since that's what the reader renders) AND the bundles** (85 sections; artifacts of record, D-011/D-017 precedent), verified `0` remaining. Follow-up passes fixed 7 ICA bodies carrying drop-cap debris + a duplicated citation ("[S T Rep. by s. 65, ibid. …"), 4 lost-space/missing-period bodies, and ICA §79's space-collapsed heading. **Deliberately NOT fixed — ICA §2/§3**: a page-boundary parse failure left §2 ("Interpretation-clause") truncated after definition (a) with footnote debris appended, while §3 wrongly holds §2's definitions (b)–(j) plus footnote text and is missing its own provision. Repairing it correctly needs the source PDF + a parser fix; **authoring the statute text from model memory is forbidden** (D-011/ADR-6 — the D-015 curated-chapter-names exception covers headings, explicitly not body text). Logged here as a known defect for the next re-ingest, alongside footnote-citation residue in the old acts (CrPC 16, IEA 26 — likely legitimate India Code footnotes, needs a human eyeball). Risk containment verified: **none of the 84 repaired sections appear in the mapping or quiz pool**, so no mapping card or MCQ could ever have shown mangled text. 46 ingest tests green; bundles re-validate; diff is exactly 169/169 lines. Revisit: fix ICA §2/§3 in the next re-ingest (highest-value content task remaining); run `content-qa` as a standing check before each release and as part of the V1.0 random-50 QA.

**D-026 · 2026-07-25 · SEO + share-card polish; OG cards were rendering a tofu box since V0.2.**
Context: fortnight item 🅒 — free, compounding acquisition while Play/domain funding is pending. **Bug found: the per-section OG card — the unit that travels in WhatsApp/Telegram study groups, i.e. the CAC=₹0 channel — has been rendering `old⇄new` as a literal tofu box (□) since V0.2.** The OG renderer (Satori) has no glyph for `⇄`/`→`; `§`, `—` and `·` are fine. Every section shared for ~a week previewed with visible breakage. Fixed to plain ASCII ("old-to-new mapping") with a comment so it doesn't regress. Also: **only section pages had an OG image**, so home, `/acts`, `/mapping`, `/daily` and `/practice` previewed blank — added a default `app/opengraph-image.tsx` (same navy brand card, wedge demo "IPC 302 is now BNS 103") which every non-section route inherits. Sitemap was missing the two new quiz surfaces — added `/daily` (changeFrequency daily) and `/practice`; `/search` stays excluded (already `noindex`, results pages shouldn't compete with section pages). Root metadata: description rewritten for accuracy and search intent — it previously promised "an AI legal tutor", which **overpromises** what we deliberately built (a grounded explainer + librarian, never an answer bot — D-023); now leads with the mapping wedge ("IPC 302 → BNS 103") and names the acts; added `keywords`, `openGraph.siteName/locale=en_IN`, and `twitter.card=summary_large_image`. Verified: both OG cards render clean at 1200×630 (screenshotted), sitemap serves **3,131 URLs** with correct production absolute URLs, robots.txt references it, typecheck clean. **Founder action required (cannot be automated):** verify the property in Google Search Console and submit `https://vidhara-web-lyart.vercel.app/sitemap.xml`. Revisit: re-check OG cards in a real link-preview debugger once a custom domain exists; re-run this audit at V1.0.

**D-027 · 2026-07-25 · ICA §2/§3 repaired from source (the last wrong-content case); corpus now 0 SEV1.**
Context: the one defect D-025 logged but refused to guess. **Root cause (from the source PDF, not inference):** ICA §2 spans the page-10 break, and India Code prints a ~20-line footnote block (Statement of Objects and Reasons, extension notifications, amendment notes 1–4) *between* definition (a) and definitions (b)–(j). The parser absorbed that footnote block into §2 and then treated footnote 3's text as the next section's heading — so **§2 was truncated after clause (a) with footnote debris appended, and §3 carried §2's definitions (b)–(j) under the heading "The words "except the State of Jammu and Kashmir" omitted by Act 34 of 2019, s", with §3's own provision missing entirely.** A student opening ICA §3 read the wrong law. Decision: repair **from the source, never from model memory** (D-011/ADR-6). Re-acquired the exact PDF — DNS via Google DoH (8.8.8.8 → Akamai edge 23.11.235.26, the host doesn't resolve locally, as D-018 found) — and confirmed it **byte-identical to the provenance record: 568,075 bytes**, sha256 `d756d45a58c4…`. Extracted §2 and §3 programmatically with `pdftotext -layout` + a script that skips the footnote block, rather than transcribing by hand; the applied §2 landed at **exactly 1,502 chars, matching the extraction**, which independently verifies the transcription. Result: §2 = intro + clauses (a)–(j), no debris; §3 = its real provision (335 chars) under the correct heading from the Arrangement of Sections, and **§3's missing Chapter I assignment restored** (it had none; §4 already had it). Applied to the live DB (version-bumped) and `bundles/ica.json`, with provenance updated on both to record the repair. **Bug caught mid-fix, worth remembering:** `UPDATE … SET body_plain = <new>, body_md = body_plain` silently writes the **OLD** `body_plain` into `body_md` (Postgres SET reads the pre-update row) — and since the reader renders `body_md`, the fix would have been invisible. Always set both to the literal, or re-sync `body_md` in a second statement. Verified: `content-qa` now reports **0 SEV1 across all 3,118 sections**; both pages render correctly in-browser (§2 shows (a)–(j) with no debris; §3 shows the communication provision); 46 ingest tests green; bundle diff 5/4 lines. Revisit: the same footnote-across-page-break pattern is the likely cause of the remaining CrPC/IEA footnote-citation residue (D-025) — worth a parser-level fix (footnote-block detection) at the next re-ingest rather than more per-section repairs.

**D-028 · 2026-07-25 · Advocates re-opened as a SECOND TRACK (not a pivot); first surface = Quick cite. Amends D-002.**
Context: founder asked to shift toward lawyers — "case diaries… efficiency… proper management system". This re-opens D-002 ("one wedge, one customer… advocates are an expansion segment") and feature-priority's **Advocate Workspace = REMOVE ("startup #2")**, so it is recorded as a deliberate amendment rather than drift. Decision: **second track, not a pivot** — the student/aspirant wedge keeps running unchanged (it has momentum: quiz, share virality, SEO), and advocate value is built as a separate, independently-validated surface. Build order chosen on cost/blocker grounds: of the candidate ideas (quick-cite, bilingual case diary, drafting aid, cause-list, billing), **quick-cite ships with zero new infrastructure**, while the genuinely differentiated one — a case diary that auto-links a case's old-law section to its new-law equivalent — needs **working accounts, i.e. the SMTP/domain work parked in D-021**, plus a private data model + RLS + reminders. Drafting/cause-list/billing were declined for now: the founder's own docs flag AI drafting as liability-heavy "startup #3", and cause-list/billing are commodity with no edge from the corpus. Shipped: `/cite` — instant lookup (reuses `parseSectionRef` + `search_sections`), **the old⇄new counterpart inline** (an advocate's most likely citation error is pasting a stale IPC section into a fresh draft), copy-citation / copy-with-text, and a **localStorage cache of every section looked up** so repeat lookups work offline on court-basement signal (explicitly NOT the full-corpus Offline bet, which still needs a service worker + bundled snapshot). Entry point is the **footer, deliberately not the primary nav**, so the student message stays sharp; it is in the sitemap with its own search intent. **Key limitation surfaced by building it: the corpus is 8 acts** (criminal codes + Constitution + Contract Act) — a district-court advocate also cites NI Act, CPC, MV Act, NDPS, Limitation… (the first test query, "138 NI Act", missed). Rather than hide this, a miss now renders the missing-content form, so the gap becomes the cheapest possible signal for which act to ingest next *for this audience*; `MissingContentForm` gained a `path` prop because it hardcoded `/search`, which would have mislabelled every advocate request as a student search and destroyed that attribution. Verified in-browser: 302 IPC → text + "now BNS §103" chip + copy actions; offline simulation → banner, cached section serves with "from cache", uncached gives an honest message. Revisit: **case diary is the real bet and it is gated on accounts** — build it when SMTP/domain lands; decide advocate expansion properly on `/cite` usage + which acts get requested (D-002's own trigger: organic advocate pull), not on enthusiasm.

**D-029 · 2026-07-25 · Case diary shipped LOCAL-FIRST — unblocks the accounts dependency instead of waiting on it.**
Context: D-028 sequenced the case diary behind accounts (blocked on SMTP/domain, D-021). Founder asked to build it now. Rather than build something unusable until SMTP lands, it ships **local-first** — the pattern bookmarks/recents/the quiz streak already use (D-007). This is not a stopgap workaround: for *this* feature it is arguably the correct design. **A case diary holds privileged client matter.** Keeping it in `localStorage` means it never reaches our servers, so we take custody of no confidential data and incur no DPDP obligations over it; it needs no account; and it works offline, which was the court-basement requirement anyway. The trade-off is real and stated plainly **in the UI, not buried**: clearing browser data erases it and it doesn't follow you across devices — hence **export/import JSON** so the data is never trapped. Server sync becomes an opt-in *upgrade* when accounts work, not a prerequisite. Shipped `/diary`: cases (cause title, court, case number, next hearing, stage, notes), **sorted by next hearing with overdue/Today/Tomorrow/In-N-days badges** (the thing an advocate actually scans each morning), delete-with-confirm, and a "N within 7 days" summary. **The differentiator — attach a section to a case and its old⇄new counterpart is resolved once and stored with it** (`IPC §302 → now BNS §103`), so a stale citation can't quietly ride into a fresh draft; chips deep-link to the full section. Refactor: the client-side section+counterpart fetch was duplicated in quick-cite, so it moved to `lib/section-lookup.ts` and both surfaces share it. Page is `robots: noindex` (personal, device-local, useless to crawlers) and reachable from the footer alongside Quick cite — still deliberately out of the primary nav so the student message stays sharp. Analytics: `diary_case_added` / `diary_section_attached` / `diary_exported`. Verified in-browser end-to-end: case created → "Tomorrow" badge + "1 case · 1 within 7 days"; `302 IPC` attached → resolved to "now BNS §103" and persisted with its marginal note; **survives a full reload**; typecheck clean. Test data cleared afterwards. Revisit: sync + hearing reminders (notifications) are the natural follow-ups and both genuinely need accounts — build when SMTP/domain lands; if advocates ask for multi-device before then, that is the strongest argument yet for prioritising the auth work.

**D-030 · 2026-07-26 · Hearing reminders — complete but inert until SMTP; the one place diary data leaves the device.**
Context: founder asked for hearing reminders, SMTP to follow. This **conflicts head-on with D-029**: the diary is local-only *because* it holds privileged client matter, but an email reminder requires the server to know the date. Resolved by narrowing rather than reversing: reminders are **opt-in per case**, and only three fields are uploaded — an address, the date, and a **label the advocate writes themselves** (pre-filled with the cause title but editable precisely so a client's name need never be sent). Notes, case number, court and attached sections never leave the device. **Double opt-in** (`confirmed_at`/`confirm_token`): nothing is ever sent to an unconfirmed address, so nobody can subscribe someone else's inbox; every reminder carries a one-click unsubscribe token that deletes *all* rows for that address. Migration 0010 `hearing_reminders`: RLS mirrors `feedback` — **anon INSERT only, no anon SELECT** (an email paired with a hearing date must not be enumerable); the scheduled function reads via service role; partial index on `(remind_on) where sent_at is null` matches the job's access pattern. Edge Function `hearing-reminders` (deployed, `verify_jwt=false` so email links work): `POST` runs the daily job behind an `x-cron-secret` header, `GET ?action=confirm|unsubscribe&token=…` serves plain HTML. **It is complete and inert** — with no `SMTP_*` secrets it returns `{configured:false}` and sends nothing, so it can be scheduled today and starts working the moment the credentials exist, no redeploy (same dark-ship pattern as the AI explainer, D-019). `sendEmail()` is the single swap point (SMTP via denomailer today; Resend/Postmark is a one-function change). `REMINDERS_MAX_PER_RUN` caps blast radius. **Bug found and fixed during verification:** `remindDateFor` parsed `${date}T00:00:00` as *local* time then round-tripped through `toISOString()` — in IST (+5:30) that shifted the result back a day, so a 31 Jul hearing produced `remind_on = 29 Jul`, i.e. **reminders two days early**. Parsing as UTC (`T00:00:00Z`) fixed it; re-verified `hearing_on − remind_on = 1`. Also regenerated DB types by hand for 0009–0010 (`ai_explanations`, `ai_usage`, `hearing_reminders`) — the CLI's `gen:types` script targets `--local` and the stored access token lacks the types endpoint, so the header now records that. Verified end-to-end: case → "Remind me" → privacy note shown → row lands with the *chosen* label (not the cause title), `confirmed_at` null, both tokens present; function returns 401 without the cron secret and renders "Invalid link" for malformed links. Test data removed from DB and localStorage. Revisit: **founder must add `SMTP_*` + `REMINDERS_CRON_SECRET` secrets and schedule a daily POST** (~18:00 IST) — see the runbook in the commit; consider Resend over Gmail SMTP if deliverability or the ~500/day cap bites.

**D-031 · 2026-07-29 · Corpus expanded for the advocate track: NI/CPC/NDPS/MV ingested and published (3,118 → 3,823 sections).**
Context: `/cite` was missing the acts a district-court advocate actually cites — the first test query, "138 NI Act", returned nothing (D-028). Founder supplied the service-role key, so four acts were ingested and published. Sources are all official India Code English PDFs, sha256 + byte counts recorded in each bundle's provenance: **NI Act 1881** (153 s., handle 15327), **CPC 1908** (171 s., handle 2191), **NDPS 1985** (126 s., handle 1791), **MV Act 1988** (255 s., handle 1798). **CrPC was NOT ingested — it was already live** (533 s. since V0.1; it is the CrPC⇄BNSS side of the mapping), so the founder's request for it was a no-op and was skipped rather than duplicated. Pipeline lessons worth keeping: (a) India Code bitstream filenames are **descriptive or `a{year}-{no}`, not the `A{year}{actno}` pattern** ICA used, and the handle page builds the link in JS — the URL lives in the DC metadata block; (b) the host does not resolve locally, so fetch via Google DoH → Akamai edge (as D-018); (c) the **`--inline` parser is correct for India Code PDFs** — the `-bbox` path yields **0 sections** on them (it is for two-column Gazette prints); (d) **NDPS exposed a schedule trap**: the parse produced 162 "sections" running to 110ZN because the Act ends at s.83 and everything after is the Schedule of psychotropic substances, whose entry numbers read as section numbers — trimmed to 126; CPC was immune because its Schedule I uses "Order I, Rule 1" form; (e) NDPS and MV handles each serve **both Hindi and English** editions — provenance records which file was taken. **Wrong-answer bug caught on verification:** `parseSectionRef` did not know the new abbreviations, so "NDPS 37" resolved to **BNSS §221** and "MV 166" to **NI §141** — a *wrong* section, which for an advocate is worse than no result. Registered all four in `ACT_ABBREVIATIONS`/`ACT_SLUG`/`ACT_ALIASES` incl. natural forms ("motor vehicles act", "civil procedure code", "narcotic drugs"). Publishing also re-introduced **12 SEV1** content defects of the D-025 repealed-section class (heading run into the citation) plus trailing leaked headings; all repaired in the live DB, version-bumped, `body_md` kept in sync. **Own error recorded for the discipline it teaches:** while clearing the last one (NDPS §5) I set the marginal note to "Definitions." *from assumption instead of the source* and duplicated an amendment note into the body — both wrong; the source gives "Officers of Central Government", and the parse had also left §5 carrying fragment text from another section's clause (iv). Corrected from the PDF. A commit message in this series also claimed "0 SEV1" before that section was actually clean — corrected in the follow-up commit. This is exactly what D-011/ADR-6 exists to prevent: **statute text comes from the source, never from recall.** Revisit: `content-qa` after every publish, not just at release — publishing is itself a defect vector.

**D-032 · 2026-07-30 · Arbitration and Conciliation Act 1996 published; STATE AMENDMENT blocks are a third distinct ingest trap.**
Context: fifth and final act of the advocate-coverage push. **ARB (26 of 1996)** — 105 sections, 15 chapters, official English PDF (690,365 bytes, sha256 `a3b936d5…`, handle 1978 `a199626.pdf`; the same handle carries Hindi `h199626.pdf`). Corpus **3,823 → 3,928** published sections across 13 acts. **New trap found and fixed before publishing:** this PDF prints four **"STATE AMENDMENT"** blocks — J&K/Ladakh insertions after s.8, s.29A and s.34, and a Bihar omission after s.43 — quoting provisions enacted by *state* legislatures. The parser read J&K's inserted **§8B as a section of the central Act** and appended amendment text to the bodies of §8, §29A, §34 and §43. Publishing that would have presented **state-only law as central law** — a wrong-content failure, not a cosmetic one. Fixed: §8B dropped (doubly wrong to keep — its sibling §8A was never captured at all), and all four bodies truncated at the marker. The Schedules (First–Seventh, incl. New York/Geneva Convention articles) did **not** leak, so the NDPS schedule trap did not recur. The remaining lettered sections (11A, 29A/29B, 31A, 42A/42B, 43A–43M) are legitimate central-act insertions from the 2015/2019 amendments. ARB registered in the ref parser (`"ARB 34"`, `"arbitration act 11"`, `"s. 9 arbitration and conciliation act"` → `/acts/arb/…`). Spot-checked against the PDF: §7, §9, §11, §29A, §34, §36. Validation 0 warnings; live pages 200; **`content-qa`: 0 SEV1 across all 3,928 sections**. Also fixed `/cite`'s coverage line, which still named only the criminal codes + Constitution + Contract Act and so under-sold the corpus by five acts to exactly the audience it serves. **Three distinct ingest traps are now on record** — dropped illustrations (D-018), numbered schedules (D-031), state amendments (D-032) — all producer-specific and all invisible to structural validation. Revisit: before the next act, add parser-level guards for schedule blocks and `STATE AMENDMENT` markers rather than repairing per-act; the service-role key used for these publishes passed through chat and **should be rotated**.

**D-033 · 2026-07-30 · Parser guards for schedules + State amendments — and the discovery that ~95 already-published sections were showing one State's law as central law.**
Context: D-032's revisit action was "add parser guards rather than repairing per-act". Doing so uncovered something much worse than the guard itself. **Guards** (`gazette-inline.ts`): `SCHEDULE_START` now also matches the **unnumbered "THE SCHEDULE"** and an optional leading "THE" on ordinals — NDPS prints only "THE SCHEDULE", so the ordinal-only pattern missed it and the substance list parsed as sections 84…110ZN (D-031); kept upper-case and anchored so body prose ("specified in the Schedule") cannot end a parse. New **`STATE_AMENDMENT` guard**: India Code prints State amendments *inline*, after the central section they modify — banner, amending text (often quoting whole inserted sections), then a `[Vide <State> Act …]` citation. The block is **skipped, not stopped at**, because the central Act resumes; it exits on the citation, or — for blocks printed without one, like ARB's first — on the first section base greater than the one in force when the block opened, which is precisely why genuine lettered sections following a block (**ARB §43A**, real central law from the 2019 amendment) survive. **Bug found while testing the guards:** both new diagnostics were nested inside `if (keepIllustrations && illustrationLines > 0)`, so they were silent for any act without illustrations — MV has six blocks and reported zero. A guard that works but cannot report is half a guard; moved out of the branch. **The real finding — this defect class was not new.** A corpus-wide query showed **~95 already-published sections** across **CrPC, CPC, IPC, MV, ICA and IEA** carrying another State's amendment appended to the central provision, present since V0.1: including **CrPC §438 (anticipatory bail), §125 (maintenance), §154 (FIR), §167 (remand), §320 (compounding)** — i.e. some of the most-cited provisions in Indian criminal practice. A reader saw, say, a Rajasthan amendment as part of the central section. 91 bodies truncated at the marker (CPC §158 fell **857,537 → 13,545 chars** — it had swallowed the Schedules). Six sections that exist **only** by State amendment (IPC 354E/376F/379B/382F/509B, IEA 114B) were labelled `[State amendment]` with a leading sentence in the body rather than deleted — they are real law where they apply, but must not read as national law. **Also fixed bundle↔DB drift**, a process defect worth naming: earlier repairs (D-025/D-031) had been applied *only* in the live DB, so republishing the bundles silently reintroduced 12 SEV1. Every repair now lives in the bundles — repeal-citation splits, bracketed chapter headings leaking into bodies, and NDPS §5 restored from the source text — and the five acts were republished from them, making bundles the single source of truth as D-011 intends. Verified: **53 ingest tests** (46 + 7 new, covering both guards plus the prose-mentions-Schedule false positive and the §43A survival case); re-parsing all five acts yields section lists **identical** to the committed bundles (zero regression); **0 SEV1 across 3,928 sections**; live pages 200. Revisit: one guard still missing — bracketed chapter headings (`[CHAPTER VA [F ORFEITURE…`) are the last class needing per-act repair; and **the service-role key used throughout should be rotated**, it passed through chat.

---

*Template for future entries:*
**D-0XX · date · One-line decision.**
Context: … Decision: … Revisit: …
