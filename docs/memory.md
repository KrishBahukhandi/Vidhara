# NexLex — Project Memory

> **Status**: Living document — the project's persistent brain. · **Last updated**: 2026-08-16
> Read this first when resuming work after any interruption. Update it whenever anything important changes. Keep newest items at the top of each list.
>
> **The open-work list lives in [backlog.md](backlog.md)**, not here. This file is state and history; that one is what remains.

---

## Current Status

- **Plan of record**: **Lean release roadmap V0.1→V2.0** — [roadmap.md](roadmap.md) + [release-plan.md](release-plan.md) + companion docs supersede phases.md (D-001…D-012 in [decision-log.md](decision-log.md)). Wedge: old⇄new law mapping for students/judiciary aspirants, with an **advocate second track** since D-028 (quick cite, case diary, limitation worksheet, documents) — a track, explicitly not a pivot. **Strategy amended 2026-08-01 (D-049): narrow and win as the *reference* product** — the corpus you can trust — rather than fight seven incumbents on practice management. `/verification` makes the provenance chain visible. No payments before Gate G2.
- **⚠️ The roadmap's calendar has slipped and has not been re-dated.** V1.0 was set for the week of 2026-09-01 while the Play 12-tester×14-day clock has still never started. Treat the release *ladder* as valid and the *dates* as stale — see backlog.md §9.
- **LIVE at [vidhara.bahukhandi-labs.com](https://vidhara.bahukhandi-labs.com)** (2026-08-08, D-064 domain move; Vercel, auto-deploys from main). Verified 2026-08-16: 200, sitemap emits the new origin, health endpoint good. The old `vidhara-web-lyart.vercel.app` is deliberately kept alive so already-shared links do not rot. All three site-origin fallbacks now default to the real domain rather than reading an env var (see [domain-and-email-setup.md](domain-and-email-setup.md) §4).
- **CORPUS: 36 acts / 5,594 sections, content-qa 0 SEV1, SEV2 29** (was 174 before the D-063 pass). Far past the "8 priority acts / 3,118 sections" this file used to record. Plus the **complete official NCRB old⇄new mapping tables: 1,271 mappings** (IPC⇄BNS 559, CrPC⇄BNSS 526, IEA⇄BSA 186), zero unresolved endpoints. Bundles in `scripts/ingest/bundles/` are the artifacts of record and are now the single source of truth — `publishBundle` deletes what a bundle no longer produces (D-052), so bundle↔DB drift is closed by construction.
- **Backend**: Supabase `eubyvglzkbzfeznocilg` (Mumbai, $0). Migrations **0001–0016** (0016 = `hearing_reminders` INSERT extended to `authenticated`, because web sign-in changes the browser's Postgres role — D-065). Four Edge Functions: `ask`, `daily-mcq`, `explain-section`, `hearing-reminders`.
- **Features shipped beyond the original V0.5 line**: AI "Explain this section" (grounded, cache-first, Groq/Llama-3.3-70B — D-019/D-020); Daily MCQ + unlimited Practice, zero-hallucination by construction from the mapping table (D-022/D-024); grounded "ask" librarian that routes to real sections and never answers the legal question (D-023); `/cite`, `/diary`, limitation worksheet incl. s.12(2), case documents (D-028…D-048); `/verification` (D-049).
- **SIGN-IN WORKS (2026-08-16, D-065)**: Resend SMTP + `{{ .Token }}` templates, and two code bugs that mail alone would not have fixed — both surfaces validated a **6**-digit code against a project issuing **8** (so the app could never have signed in either), and sign-in/sign-up were one flow, meaning a typo silently created a second empty account. Web now has a mode toggle, "no account uses that email yet", and name/role/exam-targets on sign-up. Founder confirmed end to end. **App sign-in is fixed in code but unverified on a device.**
- **🔴 NOT configured — the standing blockers**: **no Sentry** on either surface; **mobile analytics is still a stub**; **Play Console never created**; hearing reminders still dark (`hearing-reminders?action=status` → `{"configured":false}` — the Edge Function's `SMTP_*` secrets are a *separate* store from Auth SMTP); `_dmarc` still absent; **the service-role key still needs rotating** (D-032…D-063).
- **⚠️ The repo lives inside iCloud Drive and it corrupts builds** (confirmed by inode 2026-08-16). Conflict copies (`… 2.bin`, `… 3.xml`) appear inside `node_modules` and Gradle output dirs; Gradle dies hashing files it cannot read. Three Android builds failed, one after 27m54s; 468 artifacts cleared, 102 back within two minutes. `node_modules`/`.turbo` are protected by creating them **empty**, setting `com.apple.fileprovider.ignore#P`, *then* installing — retrofitting the attribute onto a synced tree does not work. `apps/mobile/android` is still exposed. See backlog §10.
- **What runs today**: six packages typecheck; 116 ingest tests, 57 shared tests; web builds; both apps fully usable signed-out (D-007 — sign-in is optional, Profile tab only).
- **PRODUCT RENAMED → Vidhara (2026-07-16, D-014)**: "NexLex" collided with an unrelated Indore law firm at nexlex.in, which was the hardcoded SITE_URL default → share/canonical/OG were pointing at a competitor. Now **Vidhara** under the **Bahukhandi Labs** umbrella. User-facing strings renamed; internal `@nexlex/*` packages + Supabase ref deliberately kept. Repo: github.com/KrishBahukhandi/Vidhara. Docs still say "NexLex" in prose — cosmetic, not load-bearing.
- **Founder build path (USB, on-device test)**: connect phone (USB debugging on), ensure ANDROID_HOME set (Android Studio SDK), `.env` present, then `cd apps/mobile && npx expo run:android`. First run prebuilds android/ + compiles + installs + launches on Metro. Gotcha (Known Bugs): after prebuild, if "SDK location not found" → add `android/local.properties` with `sdk.dir=…`; pnpm hoisted linker already set.
- **Next action**: rotate the service key (§2) → `_dmarc` + Edge Function SMTP secrets (§1) → decide the iCloud fix so builds work (§10) → Sentry + PostHog dashboard + mobile analytics (§3, §4) → *then* recruit a cohort. Play Console runs in parallel and should start immediately: 14 continuous days is calendar time that cannot be compressed.

## Completed Features

- 2026-07-13 — **Phase 0 scaffold**: monorepo (apps/mobile, apps/web, packages/{tokens,shared,db}); design-token pipeline with CI sync check; app shell (5 tabs) + OTP auth flow + onboarding + profile exemplar feature; migration `0001_create_profiles.sql` (RLS + signup trigger); `.env.example`; GitHub Actions CI (typecheck, tests, web build, Metro bundle smoke); eas.json build profiles.
- 2026-07-13 — Android-first architecture pivot documented (ADR-8/9/10) and executed.
- 2026-07-13 — Documentation foundation (the /docs six).

## Pending Features

**→ [backlog.md](backlog.md) is the list.** It is compiled from the decision log and checked against
live state, and it is organised by what blocks what rather than by phase.

The old phase-based list that sat here (Phase 0 exit items, Phases 1–8) was superseded by the lean
reset (D-001) and had become actively misleading — most of it shipped, and the parts that had not
were not blocked for the reasons it gave. `phases.md` remains as the historical build log.

## Known Bugs

*(Open items are in [backlog.md](backlog.md); these are the ones with a diagnosis worth keeping. Resolved entries are retained below because their root causes recur.)*

- **OPEN — camera + DocumentPicker paths never exercised** (D-048). Case-document attach was verified end to end on an iPhone 17 simulator down to the filesystem for the *gallery* path — the copy lands in `Documents/…/case-documents/`, the filename is regenerated from the record id so a hostile filename cannot escape the directory, and delete removes the bytes. A simulator has no camera, and the files picker was not driven. Both share `adopt()` with the proven path, but their pickers are unproven. Also: **in Expo Go the permission dialog shows Expo Go's own wording**, not the usage strings added in D-047 — those apply only in a dev/production build, so "I added them" is not "a user saw them". Check on the first EAS build.
- **OPEN — case documents have no dedupe and no size cap.** One ~1.3 MB photo per attach, growing unbounded in the app sandbox.
- **OPEN — ISR lag on content fixes.** Section pages cache 1h with no on-demand `revalidatePath` route, so a corrected section shows immediately on fresh renders but cached pages lag up to an hour (D-017).
- ~~**Dropped illustrations (153+ sections)**~~ **RESOLVED 2026-07-19 (D-018).** Re-ingested IPC/ICA/CrPC with an illustration-aware `gazette-inline.ts`: small-type lines are kept only inside an Illustration(s) block (heading→next-body-line state machine, survives page breaks, page-scoped footnote latch, letterless-line guard for superscript refs). Sources re-acquired identically (ICA/CrPC byte-identical via dns.google→Akamai; IPC from the exact Wayback snapshot 20200421003546); parity check (keepIllustrations:false vs pre-D-017 bundles) = 0 mismatches across 1,386 sections. **178 sections regained their illustrations** (IPC 74, ICA 97, CrPC 7 — more than the 153 flagged: some sections had even the heading dropped at 7.2pt); ~127 KB restored; bare-marker endings corpus-wide now 0. Published via edge-function pattern (v16 → 178/178 → tombstone v17); bundles + provenance updated. Residual proofread item (pre-existing, NOT a regression): ICA prints a few footnotes at full body height — they've always leaked into those bodies and still do (e.g. §74 area); catch in the V1.0 random-50 QA.
- ~~**Leaked section-body headings (176 sections, all 8 acts)**~~ **RESOLVED 2026-07-19 (D-017).** Group/part/chapter headings belonging to the *next* section leaked onto the END of the prior body (reported case: IPC §420 → "Of Fraudulent Deeds and Dispositions of Property"). Stripped surgically in bundles + live DB (id-preserving `UPDATE`s, `version`+1, residual scan → 0). Root cause is the same parser boundary issue as the illustrations bug; a re-ingest should also prevent recurrence.
- ~~**Android native build**~~ **RESOLVED 2026-07-14** — clean build succeeds (`BUILD SUCCESSFUL in 1m 35s`, app-debug.apk produced). Founder: plug the phone in and run `cd apps/mobile && npx expo run:android` — it will reuse the cached build, install, and launch against Metro. History: (1) "SDK location not found" → android/local.properties; (2) `ExpoLogBoxPackage` unresolved → pnpm 11 ignores .npmrc, `nodeLinker: hoisted` moved to pnpm-workspace.yaml; (3) after an interrupted install, THREE layouts coexisted (hoisted root + .pnpm store + per-app trees) and Gradle compiled stale generated code from the old .pnpm path → full clean (`rm -rf` all node_modules + generated android/) + fresh install + `expo prebuild`. Lesson: after switching nodeLinker, always clean-slate node_modules AND regenerated native dirs; local.properties must be re-written after every prebuild --clean.
- Serif font family referenced by `AppText serif` isn't bundled yet — web preview falls back to Georgia via CSS; native falls back to system font (Phase 0 polish; cosmetic).

## Design Decisions

- 2026-07-18 — **Web search + trust/demand surfaces (founder's 4 picks)**: (1) `/search` page exposing the existing `search_sections` RPC (FTS was mobile-only before) — confident refs redirect to the section, everything else gets ranked results with `**term**` snippets rendered as highlights; "Search" added to shared NAV_LINKS; landing lookup + SearchBox both fall back to /search for non-ref queries. (2) LandingLookup reused atop /mapping (SEO entry point had no way to actually look up). (3) `SectionProvenance` block on every section: official source link (India Code / Gazette-MHA), raw provenance line, and "Report an issue with {ACT §N}" → /feedback?about= which flips SuggestionForm into a scoped **report** (kind='report', Sev-0 intake, warning chip). (4) Zero-result search shows `MissingContentForm` → kind='missing' — the rows that decide which acts get ingested next. Migration 0007 extends feedback.kind (report|missing) with shape checks; all four kinds verified against live RLS (201/201/400). Verified in preview: "cheating" 20 hits, "dowry death" → the 4 canonical sections across all codes, "420 ipc" 307-redirects.

- 2026-07-17 — **Chapter grouping + copy-text + suggestions channel (web + mobile)**: chapter titles repaired corpus-wide (D-015: normalizeChapterTitle in both parsers + bundles + live DB; 56 initials-only rows curated with section-range corroboration); act pages now group sections under "CH. N · TITLE" headers (flat while filtering) — web `ActSectionList` + mobile `SectionList`; "Copy text" on both readers (web navigator.clipboard, mobile expo-clipboard — NEW native dep, added pre-first-build deliberately); suggestions: migration 0006 (`feedback.kind`), web `/feedback` page + footer link, mobile Profile `SuggestionForm` (both platforms verified end-to-end incl. RLS shape checks: suggestion-no-message 400, rating 201). 🔴 vision items (notes/highlights/progress) stay parked for the fake doors.
- 2026-07-17 — **Reading/browsing polish (web + mobile, 3 items)**: (1) **prev/next section nav** in the reader — `getAdjacentSections(actId, sortKey)` does two tiny indexed lookups (lt/gt on sort_key, limit 1); `SectionNav` component both platforms; drives sections/session. (2) **Mobile share** — RN built-in `Share.share()` (no new native dep), mapping-first text + web URL (`lib/site.ts` WEB_URL); fires share_clicked. (3) **Within-act filter** — client-side substring match on number OR marginal note, shown only when >12 sections (BNSS 531, IPC 585); `ActSectionList` (web client component) + TextInput (mobile). **Deferred**: chapter-title fix ("A RREST OF PERSONS" drop-cap artifact) + chapter grouping — the artifact is scattered mid-title and ambiguous with real words ("A COURT"); a safe fix belongs in the bbox parser via glyph-height detection + re-ingest, and titles are invisible until grouping ships. Proper task, not a small one.

- 2026-07-16 — **Web UI premium pass (landing + chrome + motion)**: rebuilt the landing page to communicate why/what/how — hero ("Two criminal codes. One place that speaks both."), concrete problem section (IPC §420 → BNS §318 visual), 3-step "How it works", "What you get" cards, credibility stat strip (3,118/1,271/8/₹0), MCQ fake door, final CTA. Motion vocabulary in globals.css (restrained, "quiet library"): `.animate-rise`/`.animate-fade` on-load, `.reveal` scroll-in (IntersectionObserver via `components/reveal.tsx`, no-JS `<noscript>` fallback + reduced-motion collapse), `.lift` hover. Responsive header: sticky + backdrop-blur, desktop nav ≥md, `components/mobile-nav.tsx` hamburger→sheet <md (Escape/route-change close, scroll lock). Richer footer (Bahukhandi Labs project line). JetBrains Mono bundled for section numbers/citations (design.md §3). All token-driven; verified desktop+mobile (375px)+dark. **Gotcha logged**: never run `pnpm build` while `next dev` is running — it clobbers the dev `.next` cache (webpack-runtime / @sentry vendor-chunk MODULE_NOT_FOUND); fix = stop dev, `rm -rf .next`, restart.

- 2026-07-13 — **ADR-11**: app consumes tokens via typed theme module (`apps/mobile/src/theme`: `useTheme()`, `sp()`, `type`) + RN StyleSheet; NativeWind dropped (unverified on SDK 57; ergonomics-only dependency). Web keeps Tailwind with the shared preset.
- 2026-07-13 — UI primitives established in app: `Screen`, `AppText` (variant/tone/serif), `Button` (4 variants, loading locks width), `Field` (visible label + inline error), `EmptyState`, chip pattern in onboarding. All colors via theme; zero hardcoded hex (grep-checked).
- 2026-07-13 — Design language: "quiet library with a sharp mind" (design.md); bottom tabs Library · Mapping · Tutor · Notes · Profile; mapping change-types color+icon.

## Architectural Decisions (ADR index — full text in architecture.md §16)

- **ADR-11 (2026-07-13)** Typed theme module replaces NativeWind on the app; Tailwind preset remains web-only.
- **ADR-8/9/10 (2026-07-13)** Android-first with Expo · single monorepo in `NexLex/` · clients use supabase-js directly, server hosts only secret-bearing surfaces.
- ADR-2 Supabase only backend · ADR-4 Postgres FTS + pgvector first · ADR-5 Claude behind `AIProvider` · ADR-6 mapping human-curated · ADR-7 prompts versioned code + DB registry.
- ~~ADR-1~~, ~~ADR-3~~ superseded.

## Important Assumptions (verify when they matter)

- **Expo SDK 57** (RN 0.86, React 19.2.3, TS ~6 default template — we pinned TS 5.9 for monorepo consistency; revisit TS 7 when whole repo can move). React/react-dom pinned to exactly 19.2.3 in BOTH apps to avoid duplicate React under hoisted node_modules — keep them aligned when upgrading.
- AsyncStorage (not SecureStore) for Supabase session persistence — session JSON exceeds SecureStore's 2KB item limit; Supabase's documented Expo pattern.
- Play Store: new personal dev accounts need 12-tester × 14-day closed test — verify founder's account status before Phase 2 ends (launch critical path).
- Supabase account facts (checked 2026-07-13): org `gsswdolhgvphznhqldrf`; existing projects UniRide (paused) + Tareshwar Tutorials (active); new project cost $0; free tier = 2 active projects, so this fits. Free projects pause after ~1 week idle. **Resolved into a live task, not an assumption:** the built-in email service is rate-limited (~2–4/hr) *and* locks the template editor, so its Magic Link template cannot be changed from a link to a `{{ .Token }}` code — which is the actual reason sign-in has never worked (D-021). Custom SMTP is therefore mandatory, not a scaling nicety. Resend is chosen and the domain is verified; see backlog §1.
- Play Billing vs Razorpay policy decision = ADR at Phase 5 start.
- Bare act texts public domain (Copyright Act s.52(1)(q)); keep provenance.
- Pricing (₹149/₹399) is a hypothesis; validate before Phase 5.

## Database Decisions

- `0001_create_profiles.sql`: enums `user_role`/`plan_tier`; profiles owner-only RLS (no INSERT/DELETE policies — signup trigger + auth cascade handle those); `touch_updated_at()` + `handle_new_user()` security-definer functions with empty search_path.
- `packages/db/src/database.types.ts` is a **hand-written placeholder** — regenerate via `pnpm --filter @nexlex/db gen:types` once Supabase exists (drop-in replacement).
- Content tables anon-SELECT; search via `search_sections` RPC; `law_mappings` edge table; migrations immutable, expand→migrate→contract.
- App offline store (Phase 5): expo-sqlite + MMKV; conflict = last-write-wins + preserved "conflicted copy".

## API Decisions

- Result contract `{ ok, data | error: { code, message } }` implemented in `@nexlex/shared` (`ok()`/`err()`, `ERROR_CODES`); profile + auth features follow it — the exemplar pattern.
- Feature APIs live in `apps/mobile/src/features/*/api.ts`; components never import the supabase client directly.
- `/api/v1/health` live (returns `minSupportedAppVersion` for the Phase 3 forced-upgrade gate). `/v1` expand-only once app ships.

## Important Conversations / Directives

- 2026-07-16 — Founder: "Ship fast, learn fast, improve fast" — rejected the phase roadmap as too big/slow/assumption-driven; requested YC-partner-grade rethink, release-based plan (V0.1→V2.0), feature triage into MUST/SHOULD/NICE/FUTURE/REMOVE, ten strategy docs as source of truth, optimized for learning/retention/feedback/speed, NOT investor optics. Executed same day (D-001…D-012).
- 2026-07-13 — Founder: Android-first launch; folder strategy delegated → monorepo (ADR-9). "Proceed" given for Phase 0 scaffold.
- 2026-07-13 — Founding master prompt: docs-first, six living documents = definition of done, challenge weak decisions, long-term maintainability.

## Future Ideas (parking lot — not scope)

- iOS app; full web app; Hindi layer; OLED-black theme; MCQ test series; moot-court mode; state amendments; judgment summarizer; institutional licensing; study groups; WhatsApp daily-section bot; push revision nudges.

## Technical Debt

- 2026-07-13 — `lint` scripts are typecheck placeholders (no ESLint configs yet). Accepted to keep scaffold velocity; payoff: Phase 0 exit polish (TODO 4). Risk: low (strict TS catches most; conventions enforced in review).
- 2026-07-13 — No error boundaries/logger util in apps yet (screens handle their own errors). Payoff: Phase 0 exit polish.
- 2026-07-13 — `packages/db` types are hand-written placeholders until Supabase project exists.

## TODO List

**→ [backlog.md](backlog.md).** The numbered Phase-0/Phase-1 list that used to sit here described a
world that no longer exists (Supabase, Vercel, the corpus and the device build all landed long ago).
Keeping a second, staler copy of the open work is how the two drift apart, so there is now one list.

## Priority Queue (order of execution)

The dependency chain, not a phase order:

```
rotate service key ─┐
                    ├─► SMTP + {{ .Token }} template ─► sign-in works ─► sync + reminders buildable
DMARC + API key ────┘
                                   │
Sentry DSN + PostHog dashboard + mobile analytics wired ─► a cohort is worth recruiting
                                   │
Play Console + 12 testers × 14 days (calendar time — start NOW, runs in parallel) ─► V1.0 Android
```

Recruiting a cohort before instrumentation is wired spends the scarcest resource — warm users — on
a week that produces no measurements.

## Recent Changes

- 2026-08-16 — **SIGN-IN WORKS (D-065)** — web sign-in/sign-up shipped, founder-confirmed end to end. SMTP was only half the story D-021 told: both surfaces validated a **6**-digit code against a project issuing **8**, so mail alone would never have produced a working sign-in, and the app has carried that since July. Schema moved to `packages/shared` accepting Supabase's whole 6–10 range, so the dashboard setting cannot re-break it. Sign-in and sign-up split (one flow meant a typo silently created a second empty account); an unknown address now gets "No Vidhara account uses that email yet" with a switch that carries the email over — **accepting email enumeration deliberately**, because silent account creation is the worse failure and an account grants nothing an attacker wants yet. Sign-up collects name/role/exam-targets into the existing `profiles` row. Migration 0016 fixed an RLS gap the session change would otherwise have opened silently. **Lesson worth keeping: "the fix is config, no code change needed" is a claim that needs testing, not reasoning** — D-021 asserted it in July and it was wrong in two places.
- 2026-08-16 — **Docs re-synced to reality; [backlog.md](backlog.md) created.** This file had drifted a month (it still claimed 8 acts / 3,118 sections against an actual 36 / 5,594) and its Pending/TODO/Priority sections described a superseded phase plan. Open work consolidated into one backlog, compiled from D-001…D-063 and **checked against live state** rather than against the docs: site 200 on the new domain, sitemap on the new origin, `hearing-reminders?action=status` → `{"configured":false}`, tags at v0.2.0, Resend DKIM/SPF/MX all resolving but `_dmarc` absent. Where docs and reality disagreed, reality won.
- 2026-08-08 — **D-064: canonical origin is `vidhara.bahukhandi-labs.com`.** Subdomain CNAME'd to the Vidhara Vercel project (per-project target — reusing the `www` one would have served the Bahukhandi Labs site instead). All three site-origin fallbacks now *default* to the real domain rather than reading an env var, so preview deploys and local dev emit correct URLs and there is no silent failure mode where an unset variable reverts every canonical link, OG tag and sitemap entry. **Gotcha worth keeping: BigRock's SOA sets a 38,400s negative-cache TTL (10.7h)** — any check run before a record existed pins "no such record" for most of a day, which is what "Invalid Configuration" against a provably-correct zone means. Remove and re-add the domain to force fresh validation; a 404 from the vercel-dns target is the *healthy* signal.
- 2026-08-08 — **D-063: the SEV2 pass, 174 → 29.** Half the number was the scanner lying (`\b` after a period can never match, so 61 "Rep. by…" bodies were false-reported; `no-terminator` rejected typographic quotes and asterisk runs). The real defect was cross-headings at the end of bodies — **the thing D-056 gave up on** — and the fix was to stop deciding as lines stream and decide **at flush**: a provision ends in a terminator, so a trailing run that follows one and has none of its own is not part of it. 124 bodies trimmed, 0 unexpected. Also found: **a regression harness that skips acts whose source is absent, silently** — the first runs covered 26 of 33 acts while appearing to cover all of them.
- 2026-08-01→03 — **Corpus 19 → 36 acts / 4,354 → 5,594 sections** (D-050…D-062), executing D-049's "breadth is the growth lever". The dominant finding across the whole series: **amendment footnotes printed at body height** are the single most common source-side defect (six acts of thirty), invisible to the height filter, and they *steal section numbers* — SC/ST lost its central punishment provision §3 that way. The fix that works is not a better filter but **acceptance gates**: the section set must equal the act's own arrangement of sections, no marginal note may be footnote-shaped, no body may retain apparatus. Run the gate check on every future ingest even when the parse looks clean.
- 2026-08-02 — **D-052/D-053: State law was live as national law.** 68 published sections carried a State's amendment inside the central provision (~142,000 chars, including CrPC §438 anticipatory bail, §125, §154) and 21 "sections" existed only by State amendment. Root cause of the *survival*: **publish only ever upserted**, so a defect fixed in a bundle stayed live for ever. `publishBundle` now deletes what the bundle no longer produces. The removed law got a surface rather than silence — `act_state_amendments`, 289 amendments across 21 States, collapsed by default, showing the amending instruction as printed and never a consolidated State version.
- 2026-08-01 — **D-049: strategy amended — narrow and win.** A market scan found the advocate case-management segment crowded by seven incumbents already shipping working OTP, sync, WhatsApp reminders and cause lists, while bare-act competitors carry 880–1,500 acts. Correction recorded to something I had told the founder earlier: **case status and cause lists are commercially available via API** (eCourtsIndia, Vakeel360) — an integration, not research; case *law* remains the expensive one. Response: compete as the *reference* product, and make the provenance chain visible at `/verification` — every number read live from the DB, and **the limits section deliberately not shorter than the claims section**.
- 2026-07-24→08-01 — **The advocate track** (D-028…D-048): `/cite`, the local-first case diary, hearing reminders (built inert), the limitation worksheet incl. s.12(2) copying time, and case documents in the app sandbox — on both surfaces. Two rules earned the hard way: the diary is local-first *because* it holds privileged client matter, so we take custody of nothing; and **a feature that silently does nothing is worse than an absent one** (D-041 — the reminder form was returning `{ok:true}` and sending no mail, so the control now hides itself behind a runtime `?action=status` check).
- 2026-07-24→25 — **AI shipped, twice gated** (D-019/D-020/D-023): "Explain this section" is grounded server-side in the section's own text plus its mapping, cache-first so each section generates once, provider-isolated (Gemini → Groq in one function when Gemini's free tier turned out to be zero). The `ask` feature is a **librarian, never an oracle** — it translates a layperson's phrasing into statutory search phrases that run against the real corpus, so every result is an actual linkable section. The open-ended answer bot stays gated behind retention (D-004).
- 2026-07-19 — **ILLUSTRATIONS RESTORED (D-018): 178 sections, ~127 KB of statute text, zero regressions.** Same-day follow-up to D-017: rebuilt `gazette-inline.ts` to keep illustration text (same ~8.2pt type as footnotes — separated by position/shape, not height: heading-opened block, ends at next body-height line, survives page breaks, page-scoped footnote latch validated against all 182 footnote blocks incl. 11 direct collisions, letterless-line guard). PDFs re-acquired identically (ICA 568,075 B / CrPC 1,879,339 B byte-match via dns.google→Akamai edge 23.11.235.26; IPC via exact Wayback snapshot 20200421003546). **Parity harness**: `keepIllustrations:false` output byte-equals pre-D-017 bundles across all 1,386 sections (0 mismatches) — proving the refactor touched nothing but illustrations; merge re-applied D-017 strips so all prior curation survived. IPC §378 now carries (a)–(p), §300 its murder illustrations, ICA §23 (a)–(k). Published via the edge-function pattern: v16 one-time-token delta publisher → 178/178 rows (version+1, body_plain recomputed) → tombstoned v17; provenance strings updated bundle+DB for all 3 acts. Verified live on the deployed site (§378 renders full illustrations). 6 new parser tests (46 total green); bundles re-validate. Scripts in session scratchpad; deltas audit at /tmp/illustration-deltas.json.
- 2026-07-19 — **CONTENT-TRUST PROOFREAD (readiness check for a 4–5 user test): 176 leaked headings stripped; illustrations bug found (D-017).** Reviewing readiness for a small test cohort, spot-checked the most-cited sections and found the reported IPC §420 body leak ("…liable to fine. Of Fraudulent Deeds and Dispositions of Property") was **systematic** — 176 sections across all 8 acts carried a trailing heading belonging to the *next* section (Of… group headings, B.—… sub-parts, [CHAPTER…]/[PART…], garbled C II.—… COI/IEA markers). Fixed surgically in `scripts/ingest/bundles/*.json` + live DB (idempotent id-preserving UPDATEs, version+1; residual scan 0; 40 ingest tests green; bundles re-validate). Detection/fix scripts in scratchpad; audit at /tmp/heading-fix-audit.json. **Also uncovered (OPEN, see Known Bugs):** the small-font footnote filter dropped **illustration text** in 153 sections (IPC 64, ICA 88, CrPC 1 — incl. §300 murder, §378 theft) → needs a re-ingest with an illustration-aware parser. Web is otherwise ready for a small web cohort; blockers for a *useful* test remain founder-account items (Sentry DSN, PostHog dashboard/cohort verify). ISR caches section pages 1h (no on-demand revalidate route) → fixes show on fresh renders now, cached pages within the hour.
- 2026-07-16 — **LEAN STRATEGY RESET (founder directive): release-based roadmap replaces Phase 1–8.** Ten strategy docs created (roadmap, release-plan, feature-priority, validation-plan, launch-checklist, analytics-plan, user-feedback-plan, success-metrics, future-ideas, decision-log); phases.md archived as build log. Core calls (D-001…D-012): ONE wedge (old⇄new mapping for students/aspirants — the corpus we already built IS the MVP); web-first validation since Play gates production behind a 12-tester×14-day closed test (clock starts V0.2); V0.1 = deploy-what-exists Fri 2026-07-18; V0.5 = 30–50-user beta (D7 ≥25% bar); V1.0 public week of 2026-09-01; AI gated behind Gate G1 fake-door + eval evidence; payments gated behind Gate G2 retention; Simulator/Moot-Court/AI-Judge/Advocate-Workspace/Drafting/Community/gamification parked in future-ideas.md with unlock conditions. North star: Weekly Returning Readers. Analytics: PostHog + Sentry + UptimeRobot, schema v1 in analytics-plan.md.
- 2026-07-16 — **~480 MARGINAL NOTES CORRECTED (BNS/BNSS/BSA) + schedule cutoff.** The proofread backlog ("~40 wrap artifacts") was actually ~480 mangled notes. Root cause: notes lead at 9.6pt vs body 12pt, so every few lines a note word shares a body line's ±4pt baseline window; the left-margin split tested word ENDS (xMax≤135) which body continuation words (x≈117.6) also pass → split failed → note words leaked into bodies. Fix: column membership by word STARTS (`LEFT_NOTE_WORD_MAX_X` 112 / `LEFT_BODY_RESUME_X` 115 — mind the dead zone: BNSS §401's "or"@106.5 was eaten by a 106 boundary). Plus: ragged-note peel (a period-less note at section start reclaims queued tail fragments — BNSS §391/392 "themselves."), section flush deferred past chapter headings (note tails wrap below "CHAPTER XXIX"; body text under a pending chapter is title material, never body), CITATION tolerates padding/stacking, bbox parser now stops at "THE … SCHEDULE" (BNSS §531 body 172 KB→1.8 KB). **Audit method: NCRB table titles as oracle** — 14 residual new-code mismatches all verified NCRB-side; old-code diffs are India Code's own body-heading typos (kept source-faithfully: IPC §312 "miscarraige", §452 "alter", CrPC §447/§409, IEA §95 — TOCs correct, body headings not). 7 parser unit tests added (35 total). Republished BNS/BNSS/BSA (IDs preserved, mappings intact at 1,271); edge fn v14→tombstone v15.
- 2026-07-16 — **COMPLETE NCRB MAPPING TABLES LIVE (1,271) + ~140 MISSING SECTIONS RECOVERED.** The official NCRB Sankalan comparative tables (SectionTableBNS/BNSS/BSA.html) are now the mapping source (ADR-6 satisfied: authoritative concordance, not model memory). New `ncrb-table.ts` parser: two redundant table directions (header rows flip column roles — detect only when act-names appear in BOTH cells; a data cell's own "-->" once flipped orientation and inverted half the table), sub-section→section aggregation, multi-old-section cells, types from cardinality + "(Change)". Migration 0004: omitted/new mappings get a null endpoint (shape+type checks, partial uniques, LEFT-JOIN view); UI renders "No corresponding provision". **Publishing the concordance cross-validated ingestion**: ~140 real sections were silently missing across 6 acts and are now recovered — (1) integer-only monotonic guard rejected every lettered insertion (IPC 498A/304B/120B…, COI Art 21A/239AA…) → sort-key comparison; (2) `/THE GAZETTE OF INDIA/i` furniture regex ate any body line saying "…notification in the Gazette of India…" (IEA §113 vanished whole) → case-sensitive; (3) IPC §17 `1[17 “Government”` (bracket ate the dot) and §174A `4 [174A .…` (body-height footnote digit) → dotless-quote fallback + footnote-digit strip + space-before-dot tolerance; (4) BSA §170 hid behind a left-margin citation ("1 of 1872.") on a recto page → citations stripped from the left zone on either page side. Live: 3,118 sections, 1,271 mappings, all landmark pairs verified via anon REST; edge function v12 (act+mappings modes) → tombstoned v13.
- 2026-07-16 — **ICA (266) + CONSTITUTION (382 arts/19 parts) LIVE — ALL 8 PHASE-1 PRIORITY ACTS REAL.** ICA runs to §266 (repealed Sale-of-Goods/Partnership stubs included). COI = [As on 9 Dec 2020] English from India Code (legislative.gov.in's hashed uploads turned out to be regional-language editions — got Kannada then Santali before pivoting; refresh COI when a newer English consolidation is found — provenance flags 105th/106th). Parser hardening: no-space numbers ("16.“Undue"), PART→chapter, preamble start (ADOPT ENACT AND GIVE), schedule terminator, and the **title-shaped guard** (line-start number + lowercase = wrapped cross-reference — had hijacked Arts 16–29 via "…of article\n30."). Arts 379–391 legitimately omitted (7th Amdt). Beware stale intermediate files: a bbox regenerated from the wrong PDF cost a debugging loop — regenerate extractions after every re-download.
- 2026-07-16 — **CrPC 1973 LIVE (484/484, 37 ch) — ALL SIX MAPPED ACTS NOW REAL.** Sourcing breakthrough: the `*.nic.in` block is **DNS-level only** — resolve via `https://dns.google/resolve?name=…` then `curl --resolve host:443:IP` hits the Akamai edge (indiacode = 23.11.x) and works instantly; NIC-origin hosts (164.100.x, e.g. lddashboard) stay unreachable (IP-level). Large files: range-chunk (`-r`) past the environment's 1 MiB download cap. CrPC's old handle 16225 is dead (site reorg; the Wayback `1974_ceiminal_AA` file was likely an Amendment Act, not the code) — current canonical: handle 15247, bitstream 15272. Inline-parser fixes: enactment regex broadened for "enacted by Parliament in the twenty-fourth Year…", and a second enactment formula mid-document (appended amendment act) now ENDS parsing. Published via edge function (v7→tombstone v8).
- 2026-07-15 — **OLD CODES: IPC (510/511) + Evidence Act (167) LIVE.** Different era, different format: pre-2023 acts use RUN-IN section headings (`302. Punishment for murder.—Whoever…`), not a marginal-note column — so a 2nd parser `gazette-inline.ts` was built (drops footnotes/superscripts by font height <8.6pt; strips amendment `[` brackets; splits title at first `.—`/`.–` with first-period fallback for repealed sections like "Definition of 'Queen'. Omitted by A.O. 1950"; never-empty note + chapter-title fallbacks). PDFs: indiacode.nic.in blocked network-wide (whole `*.nic.in` blocked here) → fetched via **Internet Archive Wayback CDX** (env caps downloads at 1 MiB — IPC 1.02MB/IEA 432KB fit). IPC §17 the one gap (`[17 "Government"` no dot). Published over seed rows; mappings intact (IPC302→BNS103, IEA65B→BSA63). **CrPC BLOCKED**: only Wayback snapshot is truncated at 1 MiB (incomplete); still on dev-sample placeholder until a complete official PDF is found. 5 of 6 mapped acts now have real text.
- 2026-07-14 — **ALL THREE NEW CRIMINAL CODES LIVE: BNS (358), BNSS (531), BSA (169)** — every section + chapter of the 2023 recodification, from official MHA Gazette PDFs. Parser rewritten to be **producer-independent** after BNSS (iTextSharp) broke the BNS-tuned (cairo) height heuristic: `gazette-bbox.ts` now groups lines by **baseline/yMax** (italics/emphasis share baseline, differ in yMin), detects note side **per page by weighing left vs right note evidence** (citations excluded — they sit right on both sides), **always extracts the right column** (a section's note can sit right even on a left page — fixed BNS §113 "Terrorist act" empty note), accepts **bare-number section starts** ("262." with body on next line), and uses a strictly-increasing plausibility guard (≤20 jump). `gazette-common.ts` = shared assembly state machine. Result: 0 gaps, 0 empty notes across all 3. Known residue: ~40 sections have multi-line marginal-note TITLES with wrap/word-order artifacts (bodies correct) → proofread task. Published via the redeployed→tombstoned edge function; upsert preserved seed IDs so all 3 mappings survive. bundles/{bns,bnss,bsa}.json committed as artifacts of record.
- 2026-07-14 — **FIRST REAL ACT LIVE: BNS 2023 complete** (358 sections, 20 chapters) from the official Gazette PDF. Key facts: indiacode.nic.in unreachable from founder's network AND Anthropic fetcher (Apify cloud probe confirmed site itself up — section pages are JS-rendered anyway); source = MHA-hosted official PDF. Parser evolution (all in scripts/ingest/src/sources/): -layout column heuristics failed (grid drift: note column wandered cols 84–98, single-space merges corrupted body) → **-bbox word coordinates + font-height discrimination (notes 8.8pt vs body 11pt) is the canonical path** (gazette-bbox.ts), with assembleSections shared state machine (gazette-common.ts) handling kerning quirks, note overflow (period-sealing), plausibility guards. Publish channel: temporary secret-gated edge function (service key server-side), bundle POSTed from disk, function tombstoned (410) after — avoids both secret handling and context flooding; `emit-sql` command added as the reviewable-SQL alternative. BNS seed rows overwritten in place (IDs preserved → all mappings intact, verified). Bundle committed at scripts/ingest/bundles/bns.json = artifact of record for proofreading.
- 2026-07-14 — **Web SEO pages + ingestion pipeline**: apps/web gains /acts, /acts/[slug], /acts/[slug]/[number] (counterpart-naming metadata + Legislation JSON-LD + no-JS statute HTML), /mapping, sitemap (25 URLs), robots, site chrome; all SSG+ISR, verified over HTTP. New `@nexlex/ingest` workspace package (bundle schema, validators + 12 tests, review-gated service-role publish, CLI). `scripts/*` added to workspace; db factory gains `persistSession` option. Hoisted relink applied after full clean (see Known Bugs history); android/ regenerated via prebuild.
- 2026-07-13 — **Phase 1 slice: content core live** — migration 0003 + sample seed on Mumbai project; `features/acts` API (search with parser-first strategy, mapping lookup); Library/act-detail/reader/Mapping screens; anonymous browsing enabled (entry → Library, sign-in moved to Profile tab); MarkdownLite + MappingCard components. Browser-verified: acts list → IPC → §302 (serif text + sample-content chip + Modified mapping card) → cross-nav to BNS §103; "crpc 154" → BNSS §173. All screens themed via tokens only.
- 2026-07-13 — **Android build attempt (founder, on-device)**: "SDK location not found" fixed via android/local.properties; second failure root-caused to pnpm isolated linking (see Known Bugs); `nodeLinker: hoisted` staged; founder chose web-preview development for now.
- 2026-07-13 — **Supabase project created and verified live** (founder confirmed $0): ref `eubyvglzkbzfeznocilg` @ ap-south-1; migrations 0001 + **0002 (new: revoke public EXECUTE on SECURITY DEFINER trigger functions — security-advisor finding)**; trigger/cascade tested with throwaway user then cleaned; types regenerated from live schema; env files written (gitignored); auth gate verified in browser against real backend.
- 2026-07-13 — **Browser verification pass**: web landing verified light+dark; app sign-in + tab shell verified via Expo web preview (mobile viewport). Three real bugs found and fixed: (1) unquoted "Source Serif 4" made the browser drop the `.font-serif` rule (preset now quotes font names); (2) hardcoded white-on-brand text was unreadable in dark mode → new `onBrand` token used by both apps; (3) AsyncStorage crashed expo-router's Node SSR on web → platform-conditional storage in the app's supabase client. `.claude/launch.json` added (web:3000, app-web-preview:8081).
- 2026-07-13 — **Phase 0 scaffold commit**: monorepo + both apps + packages + migration + CI, all green (typecheck ×5, tests 16/16, web build 102 kB, Metro bundle OK). ADR-11 recorded; docs v0.2.1 sync.
- 2026-07-13 — v0.2.0 docs sweep — Android-first pivot (ADR-8…10).
- 2026-07-13 — Created /docs six (v0.1.0); repo initialized (`66843f9`).

## Lessons Learned

- 2026-07-16 — **An independent dataset is the best validator.** Zero-gap checks on section numbers used `int()` comparisons, so ~140 LETTERED sections (498A, 304B, Art 21A…) were invisible to both the parser's monotonic guard and the gap audit — every act "validated clean" while missing some of the most-cited provisions in Indian law. Publishing the NCRB concordance (which references those sections) exposed all of them at once. Generalize: when two independent official sources must agree, joining them IS the audit — run it before declaring a corpus complete. Corollaries: never anchor-free case-insensitive regexes for page furniture (`/THE GAZETTE OF INDIA/i` deleted statute text that merely mentioned the Gazette); guards that compare "numbers" must use the domain's real ordering (sort keys), not integers.
- 2026-07-13 — Scaffold-time reality beats plan-time assumptions: Expo SDK 57 shipped a different template (src/app, native tabs, React Compiler) than the SDK 54 the plan assumed — checking the generated code before wiring styling avoided shipping an unverified NativeWind dependency (became ADR-11). Generalize: verify generated scaffolds before layering choices made on paper.
- 2026-07-13 — pnpm 11 gates dependency build scripts (`allowBuilds` in workspace yaml) — remember for future native deps.
