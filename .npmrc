# Expo/Metro requires hoisted node_modules (Metro does not resolve pnpm's
# isolated symlink layout reliably in monorepos).
node-linker=hoisted

# React Native ecosystem has long-tail peer ranges; don't hard-fail installs.
strict-peer-dependencies=false
